export enum Screens {
    CONTROLE_ESTACIONAMENTO = 0,
    HISTORICO_ESTACIONAMENTO
}

export enum PersistentStorage {
    saldo = 'saldo',
    valor = 'valor',
    historicoStack = 'historico',
    historicoStackSize = 'his-stack-size'
}